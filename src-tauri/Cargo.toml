[package]
name = "set-hosts"
version = "0.1.0"
description = "跨平台 Hosts 配置工具"
authors = ["set-hosts"]
edition = "2021"

[lib]
name = "tauri_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-shell = "2"
tauri-plugin-dialog = "2"
tauri-plugin-notification = "2"
tauri-plugin-store = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
anyhow = "1"
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1", features = ["v4", "serde"] }
tokio = { version = "1", features = ["net", "rt-multi-thread", "sync", "macros", "time"] }
log = "0.4"
encoding_rs = "0.8"
dirs = "5"
# 远程 hosts 拉取（支持 http/https/socks5 代理）
reqwest = { version = "0.12", features = ["socks"] }

# DNS 代理（feature 门控，移动端启用）
hickory-proto = { version = "0.25", optional = true }
hickory-resolver = { version = "0.26", optional = true }

# 桌面端专用插件（单实例 / 开机自启）
[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-single-instance = "2"
tauri-plugin-autostart = "2"

[features]
default = []
dns-proxy = ["dep:hickory-proto", "dep:hickory-resolver"]

[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true
