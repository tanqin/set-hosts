{
  "$schema": "../gen/schemas/ios-schema.json",
  "identifier": "mobile-ios",
  "description": "Set Hosts iOS DNS 代理权限",
  "platforms": ["iOS"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default",
    "notification:default",
    "store:default"
  ]
}
